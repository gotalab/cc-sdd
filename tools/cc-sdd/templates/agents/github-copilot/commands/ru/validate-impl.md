---
description: Валидировать реализацию против требований, дизайна и задач
allowed-tools: Bash, Glob, Grep, Read, LS
argument-hint: [имя-функции] [номера-задач]
---

# Валидация реализации

<background_information>
- **Миссия**: Проверить, что реализация соответствует утверждённым требованиям, дизайну и задачам
- **Критерии успеха**:
  - Все указанные задачи помечены как выполненные
  - Тесты существуют и проходят для реализованной функциональности
  - Подтверждена трассируемость требований (покрыты EARS требования)
  - Структура дизайна отражена в реализации
  - Нет регрессий в существующей функциональности
</background_information>

<instructions>
## Основная задача
Валидировать реализацию для функции(й) и задачи(ч) на основе утверждённых спецификаций.

## Шаги выполнения

### 1. Определить цель валидации

**Если аргументы не указаны** (`$1` пустой):
- Разобрать историю диалога на команды `/kiro:spec-impl <функция> [задачи]`
- Извлечь имена функций и номера задач из каждого выполнения
- Агрегировать все реализованные задачи по функциям
- Сообщить обнаруженные реализации (например, "user-auth: 1.1, 1.2, 1.3")
- Если история не найдена, сканировать `{{KIRO_DIR}}/specs/` на функции с выполненными задачами `[x]`

**Если указана функция** (`$1` присутствует, `$2` пустой):
- Использовать указанную функцию
- Обнаружить все выполненные задачи `[x]` в `{{KIRO_DIR}}/specs/$1/tasks.md`

**Если указаны функция и задачи** (`$1` и `$2` присутствуют):
- Валидировать только указанную функцию и задачи (например, `user-auth 1.1,1.2`)

### 2. Загрузить контекст

Для каждой обнаруженной функции:
- Прочитать `{{KIRO_DIR}}/specs/<функция>/spec.json` для метаданных
- Прочитать `{{KIRO_DIR}}/specs/<функция>/requirements.md` для требований
- Прочитать `{{KIRO_DIR}}/specs/<функция>/design.md` для структуры дизайна
- Прочитать `{{KIRO_DIR}}/specs/<функция>/tasks.md` для списка задач
- **Загрузить ВЕСЬ контекст steering**: Прочитать весь каталог `{{KIRO_DIR}}/steering/` включая:
  - Стандартные файлы: `structure.md`, `tech.md`, `product.md`
  - Все кастомные файлы steering (независимо от настроек режима)

### 3. Выполнить валидацию

Для каждой задачи проверить:

#### Проверка выполнения задачи
- Чекбокс `[x]` в tasks.md
- Если не выполнена, отметить как "Задача не помечена выполненной"

#### Проверка тестового покрытия
- Тесты существуют для функциональности задачи
- Тесты проходят (нет падений или ошибок)
- Использовать Bash для запуска тестовых команд (например, `npm test`, `pytest`)
- Если тесты падают или не существуют, отметить как "Проблема с тестовым покрытием"

#### Трассируемость требований
- Определить EARS требования, связанные с задачей
- Использовать Grep для поиска в реализации доказательств покрытия требований
- Если требование не трассируется до кода, отметить как "Требование не реализовано"

#### Соответствие дизайну
- Проверить, что структура design.md отражена в реализации
- Проверить существование ключевых интерфейсов, компонентов и модулей
- Использовать Grep/LS для подтверждения соответствия файловой структуры дизайну
- Если найдено несоответствие, отметить как "Отклонение от дизайна"

#### Проверка регрессий
- Запустить полный набор тестов (если доступен)
- Проверить, что существующие тесты не сломаны
- Если обнаружены регрессии, отметить как "Обнаружена регрессия"

### 4. Сгенерировать отчёт

Предоставить сводку на языке, указанном в spec.json:
- Сводка валидации по функциям
- Отчёт о покрытии (задачи, требования, дизайн)
- Проблемы и отклонения с серьёзностью (Критично/Предупреждение)
- Решение GO/NO-GO

## Важные ограничения
- **Учёт контекста диалога**: Приоритет истории диалога для авто-определения
- **Неблокирующие предупреждения**: Отклонения от дизайна - предупреждения, если не критичны
- **Фокус на тестах**: Тестовое покрытие обязательно для решения GO
- **Требуется трассируемость**: Все требования должны быть трассируемы до реализации
</instructions>

## Руководство по инструментам
- **Разбор диалога**: Извлекать паттерны `/kiro:spec-impl` из истории
- **Читать контекст**: Загрузить все спецификации и steering перед валидацией
- **Bash для тестов**: Выполнять тестовые команды для проверки статуса прохождения
- **Grep для трассируемости**: Искать в кодовой базе доказательства требований
- **LS/Glob для структуры**: Проверять соответствие файловой структуры дизайну

## Описание вывода

Предоставить вывод на языке, указанном в spec.json:

1. **Обнаруженная цель**: Функции и задачи, валидируемые (если авто-определены)
2. **Сводка валидации**: Краткий обзор по функциям (количество пройдено/провалено)
3. **Проблемы**: Список провалов валидации с серьёзностью и расположением
4. **Отчёт о покрытии**: Проценты покрытия требований/дизайна/задач
5. **Решение**: GO (готов к следующей фазе) / NO-GO (требуются исправления)

**Требования к формату**:
- Использовать заголовки Markdown и таблицы для ясности
- Критические проблемы отмечать ⚠️ или 🔴
- Сводка должна быть краткой (менее 400 слов)

## Безопасность и Fallback

### Сценарии ошибок
- **Реализация не найдена**: Если нет `/kiro:spec-impl` в истории и нет `[x]` задач, сообщить "Реализации не обнаружены"
- **Неизвестная команда тестов**: Если тестовый фреймворк неясен, предупредить и пропустить валидацию тестов (требуется ручная проверка)
- **Отсутствуют файлы спецификации**: Если spec.json/requirements.md/design.md отсутствуют, остановить с ошибкой
- **Язык не определён**: По умолчанию английский (`en`), если spec.json не указывает язык

### Руководство по следующим шагам

**Если решение GO**:
- Реализация валидирована и готова
- Продолжить к развёртыванию или следующей функции

**Если решение NO-GO**:
- Устранить указанные критические проблемы
- Повторно запустить `/kiro:spec-impl <функция> [задачи]` для исправлений
- Повторно валидировать с `/kiro:validate-impl [функция] [задачи]`

**Примечание**: Валидация рекомендуется после реализации для обеспечения соответствия спецификации и качества.
