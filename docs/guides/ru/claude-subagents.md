# Рабочий процесс Claude Code Subagents (Фокус на Spec-Quick)

> 📖 **English version:** [Claude Code Subagents Workflow](../claude-subagents.md)

Это руководство объясняет, как цель установки **Claude Code Subagents** (`--claude-agent` / `--claude-code-agent`) ускоряет рабочий процесс спецификаций через команду `spec-quick`. Другие команды `/kiro:*` также используют те же субагенты, но этот документ фокусируется на оркестрации spec-quick, потому что это единственная команда с субагентами, имеющая собственную логику управления.

## Краткое напоминание об установке

- Установите с помощью `npx cc-sdd@latest --claude-agent --lang <код>`.
- Файлы размещаются в:
  - `.claude/commands/kiro/` – 12 высокоуровневых команд (spec, steering, validation).
  - `.claude/agents/kiro/` – 9 определений субагентов для глубокого анализа, расширения файлов и отчётности.
  - `CLAUDE.md` – быстрый старт и советы по использованию.

## Как `spec-quick` оркестрирует субагенты

`spec-quick` — это макро-команда, которая вызывает четыре субагента последовательно — `spec-init` (inline), `spec-requirements`, `spec-design` и `spec-tasks` — для генерации новой спецификации за один запуск.

### Режимы

- **Интерактивный (по умолчанию)** – Останавливается после каждой фазы и спрашивает, продолжить ли. Идеален для первого запуска или сложных функций.
- **Автоматический (`--auto`)** – Выполняет все фазы без пауз, используя TodoWrite для отслеживания прогресса. Лучше для быстрых черновиков или функций с низким риском.

Оба режима пропускают `/kiro:validate-gap` и `/kiro:validate-design`. Сообщение о завершении напоминает запустить их вручную, если функция рискованная.

### Разбивка по фазам

| Фаза | Вызываемый субагент | Что происходит |
|------|---------------------|----------------|
| 1. Инициализация | Inline инструкции (без субагента) | Создаёт `.kiro/specs/{функция}/`, записывает `spec.json` + скелет `requirements.md` из шаблонов. |
| 2. Требования | `agents/spec-requirements.md` | Запускает `/kiro:spec-requirements {функция}` для заполнения requirements.md. |
| 3. Дизайн | `agents/spec-design.md` | Выполняет `/kiro:spec-design {функция} -y`, генерируя/обновляя `research.md` (при необходимости) и `design.md`. |
| 4. Задачи | `agents/spec-tasks.md` | Вызывает `/kiro:spec-tasks {функция} -y` для создания `tasks.md` с покрытием требований и P-метками волн. |

В автоматическом режиме команда никогда не останавливается, даже когда субагенты выдают инструкции "Следующий шаг", предназначенные для автономного использования.

### Выходные данные и пропущенные контрольные точки

По завершении вы получаете:

- `spec.json` (метаданные)
- `requirements.md`
- `design.md` (с решениями на основе исследования)
- `tasks.md` (план, готовый к параллельному выполнению)

Что **не** делается:
- Нет проверки интеграции `/kiro:validate-gap`
- Нет контрольной точки качества `/kiro:validate-design`
- Нет `/kiro:validate-impl` (реализация ещё не началась)

Планируйте запустить как минимум первые две команды валидации вручную для brownfield-работы.

### Ручной вызов субагентов

Нужно перезапустить только одну фазу? Упомяните `@agents-spec-design`, `@agents-spec-tasks` и т.д. в чате Claude Code. Эти алиасы были сгенерированы при установке и напрямую соответствуют `.claude/agents/kiro/*.md`.

## Рекомендуемый паттерн использования

1. Запустите `npx cc-sdd@latest --claude-agent --lang <код>`, чтобы убедиться, что ресурсы субагентов существуют.
2. Подготовьте память проекта через `/kiro:steering` (и опционально `/kiro:steering-custom`), чтобы субагенты унаследовали точные правила архитектуры/продукта.
3. Используйте `spec-quick <функция> [--auto]` для быстрых черновиков, затем просмотрите `requirements.md`, `design.md`, `tasks.md` как в ручном потоке.
4. Запустите команды валидации вручную, если функция затрагивает существующие системы или критические границы.
5. Продолжите с `/kiro:spec-impl` и `/kiro:spec-status` после согласования спецификации.

## Настройка поведения субагентов

1. **Начните с общих шаблонов/правил** – Обновите `{{KIRO_DIR}}/settings/templates/*.md` и `{{KIRO_DIR}}/settings/rules/*.md`, чтобы отразить чек-листы команды; все агенты и субагенты будут ссылаться на одну первичную информацию.
2. **Затем настройте промпты субагентов при необходимости** – Добавьте в `.claude/agents/kiro/*.md` эвристики компании (приоритизация, классификация рисков, политики тестирования и т.д.).
3. **Настройте триггеры команд** – Редактируйте секцию `call_subagent` в `.claude/commands/kiro/*.md` для управления условиями вызова и дополнительными ограничениями.
4. **Держите промпты краткими** – Контекст Task Tool короткий, поэтому длинные объяснения размещайте в шаблонах/правилах, а промпты субагентов пишите кратко.

## Устранение неполадок

- **Субагент не запускается** – убедитесь, что вы установили с флагом `--claude-agent` и что `.claude/agents/kiro/` существует.
- **Анализируется слишком много файлов** – отредактируйте шаг расширения паттерна файлов в соответствующем промпте субагента, чтобы сузить поиск.
- **Выходные данные отличаются от шаблонов** – обновите `{{KIRO_DIR}}/settings/templates`, чтобы сводки субагентов указывали на актуальные разделы документов.

## См. также

- [Рабочий процесс разработки на основе спецификаций](spec-driven.md)
- [Матрица установки в README проекта](../../README.md#-supported-coding-agents)
