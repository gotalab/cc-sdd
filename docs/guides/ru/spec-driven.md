# Рабочий процесс разработки на основе спецификаций (WIP)

> 📖 **English version:** [Spec-Driven Development Workflow](../spec-driven.md)

Этот документ объясняет, как cc-sdd реализует разработку на основе спецификаций (SDD) в рамках жизненного цикла разработки на основе ИИ (AI-DLC). Используйте его как справочник при выборе команды, просмотре артефактов и адаптации рабочего процесса под вашу команду.

## Обзор жизненного цикла

1. **Steering (Захват контекста)** – `/kiro:steering` и `/kiro:steering-custom` собирают архитектуру, соглашения и знания предметной области в steering-документы.
2. **Инициализация спецификации** – `/kiro:spec-init <функция>` создаёт рабочее пространство функции (`.kiro/specs/<функция>/`).
3. **Требования** – `/kiro:spec-requirements <функция>` собирает уточнения и создаёт `requirements.md`.
4. **Дизайн** – `/kiro:spec-design <функция>` сначала создаёт/обновляет `research.md` с заметками исследования (пропускается, если исследование не требуется), затем генерирует `design.md` для проверки человеком.
5. **Планирование задач** – `/kiro:spec-tasks <функция>` создаёт `tasks.md`, сопоставляя результаты с реализуемыми задачами и помечая каждую волну метками `P0`, `P1` и т.д., чтобы команды знали, какие задачи можно выполнять параллельно.
6. **Реализация** – `/kiro:spec-impl <функция> <id-задач>` управляет выполнением и валидацией.
7. **Контрольные точки качества** – опциональные команды `/kiro:validate-gap` и `/kiro:validate-design` сравнивают требования/дизайн с существующим кодом перед реализацией.
8. **Отслеживание статуса** – `/kiro:spec-status <функция>` показывает прогресс и согласования.

> Нужно всё за один проход? `/kiro:spec-quick <функция>` оркестрирует шаги 2–5 с поддержкой субагентов, делая паузу для согласования после каждой фазы.

Каждая фаза останавливается для проверки человеком, если вы явно не пропустите её (например, флагом `-y` или `--auto`). Поскольку разработка на основе спецификаций опирается на эти контрольные точки для контроля качества, сохраняйте ручные согласования для production-работы и используйте автоматическое согласование только в контролируемых экспериментах.

## Соответствие команд и артефактов

| Команда | Назначение | Основные артефакты |
|---------|------------|-------------------|
| `/kiro:steering` | Создать / обновить память проекта | `.kiro/steering/*.md` |
| `/kiro:steering-custom` | Добавить domain-specific steering | `.kiro/steering/custom/*.md` |
| `/kiro:spec-init <функция>` | Начать новую функцию | `.kiro/specs/<функция>/` |
| `/kiro:spec-requirements <функция>` | Собрать требования | `requirements.md` |
| `/kiro:spec-design <функция>` | Создать лог исследований + дизайн | `research.md` (при необходимости), `design.md` |
| `/kiro:spec-tasks <функция>` | Разбить дизайн на задачи | `tasks.md` (с P-метками) |
| `/kiro:spec-impl <функция> <id-задач>` | Реализовать конкретные задачи | Код + обновления задач |
| `/kiro:validate-gap <функция>` | Опциональный анализ пробелов | `gap-report.md` |
| `/kiro:validate-design <функция>` | Опциональная валидация дизайна | `design-validation.md` |
| `/kiro:spec-status <функция>` | Посмотреть фазу и открытые задачи | Сводка в CLI |

## Настройка рабочего процесса

- **Шаблоны** – настройте `{{KIRO_DIR}}/settings/templates/{requirements,design,tasks}.md` под ваш процесс проверки. cc-sdd копирует их в каждую спецификацию.
- **Согласования** – встройте чек-листы или требуемые подписи в заголовки шаблонов. Агенты отобразят их во время каждой фазы.
- **Артефакты** – расширьте шаблоны дополнительными разделами (журналы рисков, тест-планы и т.д.) для соответствия стандартам компании.

## Новые и существующие проекты

- **Greenfield** – если у вас уже есть общепроектные правила, зафиксируйте их через `/kiro:steering` (и `/kiro:steering-custom`); иначе начните с `/kiro:spec-init` и дайте steering развиваться по мере появления правил.
- **Brownfield** – начните с `/kiro:validate-gap` и `/kiro:validate-design`, чтобы убедиться, что новые спецификации согласуются с существующей системой перед реализацией.

## Связанные ресурсы

- [Быстрый старт в README](../../README.md#-quick-start)
- [Рабочий процесс Claude Code Subagents](claude-subagents.md)
